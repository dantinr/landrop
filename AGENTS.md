# 开发代理约定

## 项目范围

邻传是面向可信家庭局域网的轻量共享页面，要求 Node.js 20 或更高版本。服务端使用 CommonJS 和 Node.js 内置模块，浏览器端使用原生 HTML、CSS、JavaScript，没有编译或打包步骤。

- 桌面端提供公共聊天室、文件上传、上传进度、最近文件和下载。
- 宽度不超过 `900px` 时进入移动端纯聊天界面，隐藏文件面板和文件消息。
- 当前产品没有账号、口令、权限系统或上传大小限制。除非用户明确要求，不要擅自增加这些产品限制。
- 这是无外网依赖的局域网工具。前端资源应由本服务提供，不使用 CDN 或其他远程资源。

## 目录职责

- `server.js`：命令行入口，读取端口和数据目录，启动及停止服务。
- `src/app.js`：HTTP、WebSocket、静态资源白名单、安全响应头和局域网地址发现。
- `src/validation.js`：用户输入、文件名、MIME 类型和下载范围校验。
- `src/json-store.js`：JSON 数据的排队与原子写入。
- `src/file-store.js`：文件流式写入、索引、临时文件清理和空间查询。
- `src/chat-store.js`：聊天记录持久化和数量限制。
- `wwwroot/`：无需构建即可运行的浏览器界面；Lucide 是仓库内分发的第三方资源。
- `test/app.test.js`：基于 `node:test` 的集成、输入校验和版本一致性测试。
- `data/`、`node_modules/`：本地生成内容，禁止提交。

## 实现约束

- 延续现有 CommonJS 和简洁的模块边界；能用 Node.js 内置能力解决时，不新增依赖。
- 新增浏览器静态文件时，同步更新 `src/app.js` 中的 `STATIC_FILES`，并确保严格 CSP 下仍可运行。不要增加内联脚本。
- 保留 WebSocket `/ws`、文件 API 的同源校验以及现有安全响应头。即使应用只在家庭网络运行，也不能削弱路径、文件名、消息和 Range 校验。
- 文件上传和下载保持流式处理，不把完整文件读入内存。持久化写入继续使用队列和临时文件替换，避免并发写坏索引。
- 新聊天消息和在线成员中的发送者数据必须包含 `id`、`name`、`ip`；界面按 `用户名[IP]` 显示可用 IP，不为缺少 IP 的旧记录伪造地址。
- 移动端继续复用同一套页面和 WebSocket 连接，通过响应式样式隐藏 `.transfer-panel` 与 `.file-message-row`。不要删除文件相关 DOM，否则桌面事件绑定会失效。
- 前端交互保持键盘可用、图标按钮有可访问名称、文本不会造成横向溢出。移动输入框字号至少为 `16px`，并保留安全区和动态视口处理。

## 本地命令

```powershell
npm install
npm start
npm run dev
npm test
```

可用 `npm start -- --port=9000` 改端口；默认监听 `0.0.0.0:8787`。测试必须使用临时数据目录，不要读取、覆盖或依赖仓库根目录的 `data/`。

## 验证要求

- 所有改动至少运行 `npm test`；修改 JavaScript 后补充运行 `node --check` 检查相关入口文件。
- 修改服务端行为、协议、存储或校验时，在 `test/app.test.js` 增加对应的成功与失败场景。
- 修改界面时实际检查桌面和移动视口：移动端不出现横向滚动且只显示文字聊天，输入栏保持可见；桌面端聊天、上传与下载入口仍完整。
- 提交前运行 `git diff --check`，不要提交日志、上传文件、临时文件或格式化噪音。

## 版本与许可

- 根目录 `VERSION` 是版本号的唯一来源，格式为 `x.x.xx`，不带 `v` 前缀。末段从 `10` 开始；`0.0.99` 的下一版本是 `0.1.10`。
- 变更版本时同步更新 `package.json`、`package-lock.json` 顶层版本和 `package-lock.json` 中 `packages[""]` 的版本。
- 项目自身代码和文档默认使用 Apache-2.0，完整协议保存在根目录 `LICENSE`。
- 新增、升级或移除依赖时，同步更新锁文件和 `THIRD_PARTY`，记录实际版本、用途、来源、许可证和分发方式，并保留上游要求的完整许可证或 NOTICE。
- 当前运行依赖 `ws` 锁定为 `8.21.3`；前端内置 Lucide `1.44.0`。升级时同时核对许可证文件。

## Git 交付

- `origin` 指向 GitHub，`gitee` 指向 Gitee，主分支为 `main`。
- 只有在用户明确要求发布或同步时，才把同一个 `main` 提交推送到两个远端。
- 不强推、不改写已发布历史，也不提交 `data/`、`node_modules/` 或本机环境文件。
